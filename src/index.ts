/**
 * CrabCast — standalone agent orchestration for terminal AI agents.
 *
 * Placeholder entry point; the daemon core lands in a later story.
 */

export const name = "crabcast";

export function version(): string {
  return "0.1.0";
}
