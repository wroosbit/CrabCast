# Workspace {{KEY}}

You are working in the CrabCast workspace `{{KEY}}`.

This is a plain shell workspace: no tracker, no page binding. If the caller
supplied a URL as metadata, it is: {{URL}}

Everything you do must stay inside this workspace directory — it is what gets
cleaned up when the workspace is reset.
